"""Selection (router) agent — picks a specialist agent for a target + intent."""

from agents.selection.selection_agent import SelectionAgent, route_target

__all__ = ["SelectionAgent", "route_target"]
